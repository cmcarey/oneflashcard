name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: List files
      run: ls
    - name: Check docker compose
      run: docker-compose ps
